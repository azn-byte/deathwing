Drop your image files (.jpg, .jpeg, .png, .gif, .webp, .avif) directly in this
folder. The gallery page reads this folder automatically — no code changes
needed. Filenames become the image captions, so `sunset-over-lake.jpg` shows
up as "sunset over lake".
