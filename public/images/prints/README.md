Photos listed for sale on the /prints page live here. Add the image file to
this folder, then add a matching entry to `lib/prints.js` with its title,
description, and prices. Unlike the curated gallery, these need per-photo
pricing metadata, so they aren't auto-detected — each one has to be listed
explicitly.
